# If you're working with Python files:
# Make sure your file has proper Python syntax
def example_function():
    return "Hello, World!"

# If you're working with SQL files:
# Make sure your SQL has proper syntax
SELECT * FROM table_name WHERE condition;

# If you're working with JSON files:
# Make sure your JSON is properly formatted
{
    "key": "value"
}